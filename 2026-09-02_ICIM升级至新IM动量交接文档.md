# IC/IM 升级至新 IM 动量交接文档

日期：2026-09-02（Asia/Shanghai）
状态：用户已决定推进版本升级；本文是实现交接，不代表实盘下单授权。

## 1. 一句话目标

以现有“IC/IM 动量分袖 v1.2”研究候选为父版本，另建一个新版本；IC 腿保持不变，只把 IM 的中证1000动量袖从基础规则升级为当前 A 股多头 v1.3 的完整规则：

```text
MA35 / Mom18 / W2.5
+ Score > 0
+ 50% OFF + 50% Abs20 > 0
+ 前一信号日 Score >= 150 时清仓
+ Volume / MA160 >= 0.85 才保留信号
```

推荐版本名：`IC/IM 动量分袖 v1.3`。不要原地改写 v1.2，也不要修改冻结的 `IC/IM 系统主线 V2`。

## 2. 必须先分清的版本边界

本次升级对象是以下文件族代表的“带动量袖 v1.2”：

- `im_mainline_v1_2.py`
- `ic_im_mainline_v1_2.py`
- `poe_ic_im_mainline_v1_2_bot.py`
- `poe_ic_im_v1_2_state.py`
- `poe_ic_im_v1_2_server.py`
- `run_ic_im_v1_2_github_digest.py`

`notes/mainline_registry.md` 与 `docs/ic_im_system_mainlines_v2_spec.md` 所登记的冻结 V2 是另一条正式研究主线。冻结 V2 的 IM 没有多头动量袖，只使用中证1000 `MOM120 < 0` 决定 Put 下限。本次不得修改冻结 V2 的规范、首次输出、SHA-256 或现有逐日路径。

如果最终希望把新动量袖并入冻结 V2，必须另做一套“V2 + 动量袖”的预注册、资金重组、回测和版本冻结；不能把本次 v1.2 父版本测试直接冒充为 V2 结果。

## 3. 新旧 IM 动量规则

|模块|原 IM v1.2 动量袖|新 IM 动量袖|
|---|---:|---:|
|偏离均线|MA35|MA35|
|动量斜率|Mom18 / W2.5|Mom18 / W2.5|
|基础门槛|Score > 0|Score > 0|
|绝对动量|50% OFF + 50% Abs20 > 0|相同|
|过热处理|无|前一信号日 Score >= 150，下一执行行清仓|
|成交量过滤|无|Volume/MA160 >= 0.85|
|成交量暖机|不适用|160日均量未就绪时放行|
|动量权重|0 / 0.5 / 1.0|0 / 0.5 / 1.0|
|动量 IM 名义|0.5 × 动量权重|相同，即0 / 0.25 / 0.5倍|

权威规则来源：

- `D:/动量策略/A 股股指多头策略/poe_cn_four_index_raw_momentum_combo_v1_3_bot.py`
- 中证1000配置：`zz1000`
- 交接时实现哈希：`bf7306f6cb4c7f467bb8db05bab3f18e49e65afe226f3007f0da4eff18319fb5`
- 策略规格哈希：`ab183ddc1e5834cb344347e83e4ba0ffd9adbc2c4c2cb9e34eb662b1bade6c46`

不要重新手写一个“看起来相同”的近似公式后直接上线。新 IC/IM 版本必须与上述 A 股 v1.3 中证1000袖逐日对账。

## 4. 时点语义必须原样继承

### 4.1 基础与成交量过滤

在信号日 T：

1. 用 T 日收盘价计算 Score 与 Abs20。
2. 用 T 日累计成交量除以包含 T 日在内的160日平均成交量。
3. 均量尚未形成时放行；形成后只有 `Volume/MA160 >= 0.85` 才保留 T 日基础目标。
4. T 日过滤后的目标只作用于下一共同交易日的 IM 动量仓位。

历史路径不能让 T 日成交量影响 T 日已经获得的 close-to-close 收益。

### 4.2 Score 过热

执行行 D 使用前一信号日的 Score：

```text
Score(T) >= 150 且下一执行行原本有仓
=> D 行动量权重清为0
```

不得改为使用 D 日收盘后才知道的 Score 去抹掉 D 日收益。

### 4.3 实时/盘中成交量

旧 Poe 入口的 `fetch_price_history()` 只返回收盘价序列，无法实现新规则。必须改为经过验证的 OHLCV 历史链，至少包含：

```text
date, open, high, low, close, volume
```

盘中“若现在收盘”不得用价格字段、期货成交量或随意缩放值代替中证1000指数累计成交量。应复用 A 股 v1.3 已验证的 OHLCV、成交量单位检查和实时占位语义；如果无法取得可验证成交量，应明确降级或失败关闭，不能静默按“通过”处理。

## 5. 明确保持不变的 IC/IM 结构

本次只替换 IM 动量权重路径。以下全部保持父版本口径：

- 固定核心 IM 名义：0.5倍。
- 动量 IM 名义：`0.5 × 新动量权重`。
- 独立网格：原触发阈值、T+1开盘执行和0/1倍名义不变。
- Put：只覆盖0.5倍固定核心袖，动量袖与网格仍为0张。
- Call：只覆盖0.5倍固定核心袖，动量袖与网格不卖 Call。
- Put/Call合约选择、D10、IV26%、5%威胁救援和期限语义不变。
- 每1倍期货30%保证金/风险缓冲不变。
- IC/IM 系统剩余现金继续按年化3%核算。
- IM期货单边成本继续使用父版本 `ONE_WAY_COST=0.0001`，并保留滚动时双边成本。
- A 股 v1.3 的2%现金收益和0.10%成本不移植；那是 A 股多头策略自己的收益口径，不是本次要复制的动量信号。
- IC 腿的目标、收益、Put、网格和 Call 排除规则全部不变。

## 6. 已完成的替换测试

研究运行目录：

`quant_param_scan_runs/20260902_ic_im_im_mainline_v1_2_im_1000_vs_a_v1_3/`

关键产物：

- `record.md`
- `scan_summary.csv`
- `window_metrics.csv`
- `parity_checks.csv`
- `daily_outputs/baseline_basic_momentum.csv.gz`
- `daily_outputs/candidate_current_a_v1_3.csv.gz`
- `daily_outputs/weight_comparison.csv.gz`

### 6.1 完整替换结果

|窗口|原基础动量 年化/MaxDD|新完整v1.3 年化/MaxDD|年化变化|回撤改善|
|---|---:|---:|---:|---:|
|全样本|27.85% / -34.44%|30.55% / -26.57%|+2.69pp|+7.88pp|
|10Y|29.91% / -26.72%|31.77% / -25.89%|+1.86pp|+0.83pp|
|5Y|44.06% / -26.72%|46.45% / -25.89%|+2.40pp|+0.83pp|
|3Y|55.81% / -26.72%|60.23% / -25.89%|+4.42pp|+0.83pp|
|1Y|62.19% / -10.72%|62.01% / -10.72%|-0.18pp|0.00pp|
|真实IM/MO区间|43.89% / -26.72%|46.95% / -25.89%|+3.06pp|+0.83pp|

全样本为2015-04-16至2026-08-14，共2,756行。真实IM/MO区间为2022-07-22至2026-08-14，共986行。

### 6.2 单模块归因

|候选|全样本 年化/MaxDD|真实IM/MO 年化/MaxDD|
|---|---:|---:|
|原基础动量|27.85% / -34.44%|43.89% / -26.72%|
|只加Score>=150清仓|29.98% / -28.34%|46.36% / -26.72%|
|只加Volume/MA160>=0.85|29.10% / -32.82%|44.47% / -25.89%|
|两者全部加入|30.55% / -26.57%|46.95% / -25.89%|

完整过滤链相对基础路径改变317个执行日，其中Score清仓32日、成交量过滤285日、重叠0日；所有差异都是新路径降低仓位，没有任何一天提高仓位。平均动量权重由0.412降至0.332，换手增加，但上述净收益已经计入相同成本公式。

### 6.3 已通过的基线与时点检查

- 重建基线逐日收益对旧定值曲线最大绝对误差：`9.974659986866641e-17`。
- 原 IM v1.2 动量权重对 A 股 v1.3 基础部分最大绝对误差：`0.0`。
- 基线、Score单项、成交量单项、完整候选的 Put 未解释时点差异：均为0。
- 2023-03-06的1.5张Put差异已经被严格识别为延迟到2023-03-07、且有真实旧腿成交证据的可解释执行事件；不得退回旧版“记录差异但继续当作完全匹配”的弱校验。
- 参数扫描产物通过完整阶段严格检查。

## 7. 结果限制

- 2015-04-16至2022-07-21使用 `model_avg_basis`，该代理用上市后991个交易日均值回填，含前视，只能作为参考压力路径，不能称为样本外或真实历史。
- 真实IM/MO只有2022-07-22以来约4年；虽然候选年化提高3.06pp、回撤改善0.83pp，但长期回撤改善7.88pp主要来自含代理的全样本。
- 最近1年候选年化低约0.18pp，最大回撤不变。
- 当前结论支持升级方向，但这些结果仍是 v1.2 父结构上的研究证据，不是冻结V2的新绩效。

## 8. 建议的新版本文件族

不要覆盖旧文件。建议新增：

- `docs/im_mainline_v1_3_spec.md`及对应SHA-256。
- `im_mainline_v1_3.py`。
- `ic_im_mainline_v1_3.py`。
- `poe_ic_im_mainline_v1_3_bot.py`。
- `poe_ic_im_v1_3_state.py`。
- `poe_ic_im_v1_3_server.py`。
- `run_ic_im_v1_3_github_digest.py`。
- 对应的 `test_*_v1_3.py`。
- 新的版本化输出目录；不得覆盖 `outputs/im_mainline_v1_2/`、旧定值输出或旧账本。

Poe必须继续保持单文件自包含。不得让Poe上传文件依赖本地跨目录 import；可通过经过校验的同步/生成流程把权威动量函数嵌入Poe文件，并用源码哈希和逐日权重对账防止漂移。

## 9. 实现顺序

1. 预注册 `im_mainline_v1_3_spec.md`，写清本文件第3至第5节规则。
2. 从 v1.2 复制出新版本文件族，不修改旧规范和旧输出。
3. 本地实现接受并严格验证中证1000 OHLCV，而不是只接受 close。
4. 复用当前 A 股 v1.3 的中证1000完整过滤链；先做到逐日权重完全一致。
5. 保持核心、网格、Put/Call、现金、保证金和成本链不变，重建目标日程与绩效。
6. 同步 Poe 的历史、实时、参数解释、信号解释和失败降级路径。
7. 建立新账本版本并完成迁移验证。
8. 更新 GitHub/Gmail runner 指向新版本，但先做dry-run；不得让测试运行发送真实邮件。
9. 完成聚焦测试、全量回归、正式表现页和最新收盘信号对账。
10. 用户确认切换后，再切换服务入口和定时任务；切换不等于自动下单授权。

## 10. 账本迁移要求

策略语义变化后，不得把新信号直接续写进旧 v1.2 哈希链并继续称为同一账本。

推荐做法：

- 旧 v1.2 状态目录和哈希链只读保留。
- 新 v1.3 建立独立状态目录、版本号和genesis/migration记录。
- 以切换前最后一个已确认交易日为锚点，从确认OHLCV回放新v1.3动量规则。
- 新账本中的“当前动量权重”必须来自上一共同交易日的新规则信号，不能直接照抄旧账本的 `verified_next_momentum_weight`。
- 记录旧账本最终摘要、新规则源码哈希、OHLCV数据截止日、回放区间和新账本初始摘要。
- 对切换日可能出现的动量仓位差异明确生成一次版本迁移调仓说明；不得伪装成普通日内信号。
- 回滚时切回旧服务和旧状态目录，不反向改写任何一条哈希链。

## 11. 最低测试清单

### 11.1 动量语义

- MA35/Mom18/W2.5、Score、Abs20逐日对 A 股 v1.3。
- `Score>=150`必须使用前一信号日值，并在下一执行行清仓。
- 成交量过滤必须使用信号日 `Volume/MA160`，暖机期放行。
- 成交量不足、单位异常、日期错位、重复日期和过期数据全部失败关闭。
- 输出权重只允许0/0.5/1；IM动量名义只允许0/0.25/0.5倍。

### 11.2 组合不变量

- IC逐日目标、收益和交易事件对父版本误差为0。
- IM固定核心始终0.5倍；网格逐日状态对父版本误差为0。
- Put/Call只覆盖核心，动量和网格的Put/Call数量恒为0。
- 除动量期货成本、总名义和由此变化的现金外，其余组件逐日对父版本不变。
- Put可解释延迟事件保留，未解释差异必须为0。

### 11.3 本地、Poe与账本一致性

- 本地历史目标、Poe历史表现、Poe收盘信号、Poe实时参数使用同一套新规则。
- Poe在无 `__file__`、无本地路径环境下可执行。
- 收盘确认信号才能推进账本；盘中查询不得写账本。
- IC和IM只有同一交易日都完整成功时才能推进统一账本。
- 状态迁移、崩溃恢复、重复请求和逐日补账保持幂等。
- GitHub/Gmail附件必须来自已通过哈希链校验的同一账本记录。

### 11.4 回归与表现

- 先复现本交接文档第6节冻结结果，再刷新到实施日最近已确认交易日。
- 输出Full/10Y/5Y/3Y/1Y和真实IM/MO区间。
- 报告年化、最大回撤、波动、Sharpe、平均动量权重、换手和成本。
- 最近1年允许小幅落后，但必须明确展示，不能只报告长期窗口。
- 全测试套件通过后才能进入切换步骤。

## 12. 验收门槛

升级只有同时满足以下条件才算完成：

1. 新版本预注册完成，旧v1.2和冻结V2均未被覆盖。
2. 新IM动量执行权重对当前A股v1.3中证1000权威路径逐日最大误差不超过`1e-12`。
3. IC路径完全不变；IM核心、网格、Put/Call组件逐日不变。
4. 基准收益复现误差不超过`1e-12`，候选绩效可由新版本正式入口重算。
5. 所有成交量字段通过单位、日期、完整性和新鲜度验证。
6. 本地、Poe、服务、账本和日报没有旧基础动量公式残留。
7. 新账本迁移记录完整，旧账本可独立验证和回滚。
8. 聚焦测试、全量回归和Poe执行环境测试全部通过。
9. 参数页、信号页、表现页均明确写出Score150与MA160/0.85规则。
10. 切换前再次取得用户确认；没有自动或人工下单授权时，只发布研究信号。

## 13. 建议复核命令

```powershell
python -X utf8 -m pytest test_im_mainline_v1_3.py test_ic_im_mainline_v1_3.py -q
python -X utf8 -m pytest test_poe_ic_im_mainline_v1_3_bot.py test_poe_ic_im_v1_3_state.py test_run_ic_im_v1_3_github_digest.py -q
python -X utf8 -m pytest -q
python -X utf8 <新版本正式绩效重建入口>
```

禁止用占位脚本或伪造CSV满足上述命令；命令名称应在实施时替换为真实新增入口。

## 14. 交接结论

现有证据支持把完整中证1000 v1.3过滤链升级进 IM 动量袖：全样本和真实IM/MO区间均提高年化并降低最大回撤，Score与成交量模块单独测试也都没有破坏主要窗口。实施时最重要的不是再复制三个参数，而是同时完成OHLCV数据链、T+1时点、Poe自包含路径、状态账本迁移和所有下游展示的一致升级。

这项工作应作为新版本实施，不能在旧v1.2上静默改公式，也不能修改冻结V2后仍沿用原版本标识。
