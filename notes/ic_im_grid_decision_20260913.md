# IC / IM 网格参数决定（2026-09-13）

状态：用户已确认参数，并授权写入正式研究脚本、同步远端与本地/GitHub日报。发布规格见 `docs/ic_im_grid_half_20260913_v1_spec.md`；完成状态以发布验收记录为准。

| 品种 | 进入线 | 退出线 | 网格权重 |
|---|---:|---:|---:|
| IC | 0.5 | 1.0 | 0.5 |
| IM | 0.9 | 1.7 | 0.5 |

进入为自身估值分数 <= 进入线，退出为分数 >= 退出线；沿用原 T 收盘信号、下一交易日执行约定。权重 0.5 指各品种网格新增期货敞口由 1 倍降至 0.5 倍，不是 IC/IM 组合分配比例。底仓和动量权重不因本决定变化。IC 无 Call；网格不新增期权覆盖。

## 决策证据与范围

- IC：`quant_param_scan_runs/20260913_ic_balanced_half_grid/window_metrics.csv`。
- IM：`quant_param_scan_runs/20260913_im_balanced_half_grid/window_metrics.csv`。
- 现有证据截至 2026-08-14，为固定历史组件对比；不是最新 r7 全规则完整回测。
- IC 模型/真实分段涉及 Put 数据来源，IC 期货行情全周期为真实数据。IM 模型期采用重建自身估值与原平均贴水情景；平均贴水有前视局限，0.9 进入线在模型区未触发网格。
- 尚未完成这组参数同时应用后的最新版完整回测与组合比较。参数确认不表示该验证已经通过。

原冻结规格、首次正式输出、r7 账本与发布入口保持原有状态；本决定不构成实盘下单或对外发布授权。
