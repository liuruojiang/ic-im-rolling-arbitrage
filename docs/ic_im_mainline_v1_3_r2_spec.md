# IC/IM 动量分袖 v1.3 r2 统一规格

预注册日期：2026-09-03（Asia/Shanghai）  
状态：研究候选；未批准实盘；不修改冻结V2、v1.2或v1.3 r1首次输出。

v1.3 r1仅同步IM，未完整覆盖用户要求，保留为`im_only_draft_superseded`审计记录。r2是本次升级的完整候选：IC使用`ic_mainline_v1_3`，同步中证500的6%基础NAV回撤后半仓防守；IM继续使用`im_mainline_v1_3`，同步中证1000的Score>=150清仓和Volume/MA160>=0.85过滤。两腿均以A股多头v1.3当前实现逐日对账。

除两腿动量权重及其直接派生仓位、期权保护数量、现金与收益外，IC/IM父版本结构不变。IC无Call；IM Put/Call只覆盖0.5倍核心袖，救援期限保持`rescue_next_listed`；每1倍期货按30%保证金/缓冲。统一入口不定义IC与IM资本配比，不生成订单。

v1.3 r2必须使用独立构建号和带`strategy_revision=r2`的账本。r1账本只读归档；r2从v1.2最后核验日以前的创世检查点按两腿新规则逐日重放，不得把r1或v1.2的下一动量权重直接复制为r2状态。现有服务或自动化的切换仍需单独确认。
