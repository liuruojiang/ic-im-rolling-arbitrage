# IC/IM v1.4-r1 fix5：卖 Put 满足条件可持续展期

规则生效边界：2026-09-26 信号日（其后首个实际运行交易日生效）。构建 `v1.4-20260926-r1-coreput3x-fixedshort95-fix5-repeatroll-iciv30-qdelta05`；规则修订 `ic_im_v1_4_repeat_short_put_roll_20260926_v1`。schema 4、strategy 1.4、revision r1 和前向哈希链不变。2026-09-25 及以前信号保持当时生产者身份和原“一次成功展期”规则，旧账本及已发送日报不得改写。

## 规则范围

- IC 固定核心卖 Put 仍要求估值 0/1 档、原执行动量许可、M+1 标准 510500 约 95% Put、自身 IV 严格大于 30%、`q_delta05`、权利金较当前腿入场价衰减至少 50%。IM 仍要求估值 0/1 档、MOM120 非负、M+1 MO 约 95% Put、自身 IV 严格大于 35%、`q3_delta05`、当前腿权利金衰减至少 60%。
- 每次成功展期之后，以**新腿**入场权利金及实际到期日重新判断；下一次仍满足上述全部准入条件即可再展期，不设成功次数上限。未成功的尝试不计成功展期，待展期状态保持；新腿 IV 或正价报价不合格时等待，不自动换为不合格腿。到期/结算分流仍优先于展期。
- 既有 `v14_early_roll_used` 对 2026-09-25 及以前信号保持一次成功展期语义；新规则日即使存量短 Put 此标志为真，也允许在独立满足条件后再次展期。新规则日成功展期后该标志恢复假值，使下一腿可再次检验。事件、合约与哈希链仍可用于审计成功次数；不回填历史状态。
- 用户明确采用做市商纸面成交假设，IM 卖 Put、D10 Call 和 Call 救援的正价、同日可核验报价不再因历史成交量或持仓量为零而被否决；价格、IV、期限、风险门槛仍需满足。这是模型报价准入，不声称经纪商已实际成交或可按任意张数成交。
- 其余买 Put、网格、Call 目标、季度换仓、手续费、现金利息、30% 绩效缓冲及 15% 用户资金可行性假设均不变。IC 仍无 Call；不授权实际下单、交割或账户持仓变更。

## 验证与发布边界

L6 同窗基准必须同时含 IC/IM 不卖 Put、原最多一次和持续展期，并单独披露持续展期相对不卖 Put 的收益与最大回撤。真实挂牌样本的经济改善不等于独立样本外确认。合成同周期两次成功展期、历史日期一次上限、待展期、缺候选报价、零历史成交量/持仓量正价报价、版本身份和状态续接均须通过回归。云端固定提交、批量/增量一致性、重启恢复和影子运行属于 L7，不得把本规格或本地代码通过冒充 L7 已过。
