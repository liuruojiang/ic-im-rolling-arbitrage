# 外部评审修复与验证计划（2026-09-11）

范围：当前本地 v1.3-r7 研究查询、账本读取与报告；保留已有未提交修改。备份：`.codex_backups/20260911_002234/`。不修改冻结规格、历史链、策略参数，不部署或发送邮件。

## 1. 修复

- IC当前Put分袖张数从哈希核验记录的信号恢复；旧锚点仅在零仓或单一分袖时确定性恢复，双袖缺证据拒绝猜测。今日Delta只计算目标数量。
- 当前保护状态统一取账本；同日查询使用当日已核验状态，下一未核验交易日使用前日的下一开盘权重/网格状态。
- 收盘前close查询明确提示尚未收盘；已完成日线不再被实时价覆盖。收盘后的日线缺失仍阻断，禁止替代。
- 期权T-1预告使用close_confirmed；IM月度执行继续仅T0并保留去重。
- 完整build调用统一运行时钟；传输重试共享总截止时间，保留数据错误不重试。
- IV午休校验扣除11:30—13:00休市时间，维持20分钟活跃交易时间容忍及来源日期检查。
- 更正绩效未启用提示、图表标题、季度换仓文字、IC最终权重范围及相对估值冻结日期。

## 2. 再检测

新增针对回归：Delta变化不改变当前数量；双袖数量恢复和不一致阻断；同日/桥接状态区分；盘中close不取行情；正式收盘不覆盖；T-1事件不随查询时段变化；重试不延长截止时间；午休新鲜度及复市过期。

运行相关信号、账本、服务/日报、行情校验、IM执行及IV测试。失败按真实契约定位，不通过放松断言掩盖问题。

## 3. 再验证

读取真实本地账本并核对哈希、IC分袖总和，验证读取不改写历史。使用真实本地指数历史验证收盘保留行为；正式查询入口在硬超时内只读运行，记录数据源阻塞。保存测试结果、代码差异检查和未完成的网络验证项。

暂不实施：133行正式降级、弱化多源审计、零买价可成交假设、244/252统一、月包替代接口、未确认字段映射及策略晋级。

## 执行状态

2026-09-11：本批1—3阶段完成。300项相关测试通过；最终代码哈希与回归版本一致。最终版本正式入口只读联网复核于00:34:08开始，IC/IM均成功返回2026-09-10研究信号，原账本及隔离副本均未变。验收与限制见 `outputs/external_review_fix_20260911/record.md`；最终联网证据在其 `final_verification/` 子目录。不把本批验收扩大为未列入实施范围的接口改造或部署验收。
