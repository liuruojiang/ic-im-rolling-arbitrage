# IC / IM v1.4-r1 对抗修复 fix1 规格

修复日期：2026-09-17（Asia/Shanghai）  
构建：`v1.4-20260917-r1-coreput3x-fixedshort95-fix1`  
规则版本：`ic_im_v1_4_coreput3x_fixed_short95_20260917_v1`  
账本：schema 4、strategy `1.4`、revision `r1`  
生效边界：仍为2026-09-18信号日

## 1. 边界

本修复不改变冻结r1的策略参数、准入阈值、数量口径、费用、换仓、绩效或生效日期。原规格`docs/ic_im_mainline_v1_4_r1_spec.md`、其SHA-256和原发布记录保持不变。本文件只登记对正式实现、状态校验、迁移和报告身份的前向修复。

本地研究信号代码和新测试已更新；未修改既有v1.3/v1.4正式账本，未发送邮件、未推送GitHub、未部署Poe/Modal、未切换Server URL、未生成订单。

## 2. 修复合同

1. 恢复期已持有的0.5倍期货计入当前仓位；连续恢复日不得重复显示加仓。
2. 核心Put三倍兑现只在T收盘确认后记录，执行日固定为下一共同交易日；仅该执行日收盘的有效重建价格可以完成事件。月度正常维护优先于已有三倍pending。
3. IC核心Put与动量Put使用独立持久身份和数量。月度重置及三倍重建记录新核心合约、证券ID、数量和实际可验证价格；动量合约不被核心重建覆盖。核心数量归零时清除旧成本、资格和pending。
4. IC卖Put候选必须校验行情日期；历史补账没有对应历史候选链时失败关闭，禁止调用当前链替代。
5. 卖Put准入和持久状态拒绝NaN/Inf、非法合约、错误到期日、非正权利金、缺失cycle/event、混版生产器及核心买卖Put/Call互斥冲突。
6. IC分袖数量在迁移后显式保存；迁移必须通过新账本锚点恢复检查。
7. 路由切换同步更新Put数量、合约和动作。日报标题、结果身份及网格文案统一为1.4和0.5倍当前规则。

## 3. 到期结算证据限制

当前仓库没有经核验的官方期权结算/交割结果和整轮含成本恢复凭据生产器。fix1明确输出`blocked_missing_official_settlement_or_cycle_cost_evidence`并保持等待，不从日行情推测价外失效、行权、ETF交割、现金结算或回本。纯策略和账本已支持经验证字段，但正式自动来源尚未接入，因此不能宣称卖Put完整生命周期已经端到端闭环。

在官方证据源接入前，外部发布或部署必须保留该限制；不得用手改账本或假定结算绕过。该阻塞不影响生效前旧规则续接，也不改历史研究绩效，但会阻止实际到期周期自动进入恢复阶段。

## 4. 验收

- 生产模块编译通过。
- v1.4新增及继承回归262项通过；仅有既存第三方弃用警告。
- 同一冻结研究引擎和数据快照重新运行IC、IM历史研究；58个CSV/压缩CSV、119,527行逐表相等，最大数值差0。
- IC真实挂牌窗口仍为2022-09-19至2026-08-14；IM真实挂牌窗口仍为2022-07-22至2026-08-14。理论段仍为理论延展，不能视作可成交历史。
- 详细记录：`outputs/v1_4_fix_20260917/record.md`；历史比较：`outputs/v1_4_fix_20260917/historical_impact.json`。

## 5. 回退

风险逻辑修改前备份位于`.codex_backups/20260917_112337`，登记表备份位于`.codex_backups/20260917_112818`。回退只允许建立新的前向修订；不得改写冻结规格、首次输出或既有哈希链账本。
