# IM 新动态估值三档 MO Put v10 判定失败记录

日期：2026-08-19  
状态：实现审计失败；不得引用v10的晋升门槛结论

## 失败原因

v10真实层主候选在全样本和最近3年的MaxDD与no-Put实质完全相同，但浮点重算分别产生约`8.77e-13`和`2.22e-13`个百分点的正差。v10判定函数使用严格`> 0`，把这两个数值噪声误记为“回撤改善”，从而错误显示真实层三个可用窗口全部改善。

该问题不改变逐日收益、Put损益、交易记录、成本、收盘价奇偶或候选指标的数值，只污染门槛布尔值、宽度方向和稳定性标签。

## 处理

- 不覆盖`outputs/im_mo_adaptive_valuation_tier_put_v10/`；保留其逐日和交易产物作为失败审计证据。
- 不修改v10脚本，以维持正式产物中的脚本哈希可复核。
- 新建v11归一化审计，预注册“MaxDD改善至少0.01个百分点”才算有经济意义；候选、数据、执行、成本和逐日路径均不改变。
- v10不得作为参数晋升或实盘依据。

