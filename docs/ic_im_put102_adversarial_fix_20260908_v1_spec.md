# IM Put102 对抗审计修复规格

2026-09-08用户明确授权修复已复现问题，并延续本地/远端日报同步要求。策略参数保持MOM120条件及双Put102%；不新增IV减仓规则。

- 同一IM Put月度事件仅维护一次；持久锚点新增可选已维护事件日期，旧记录不追写。月底前一交易日收盘形成次日目标后，同一事件在到期日不重复选择；若漏跑则仍按原逐日补账补齐。
- 保留官方MO原收盘、成交量及当日结算。历史/收盘零成交量使用官方结算估计并披露，不能宣称已成交；IC/IM期货与Call执行规则不擅自改动。IM Put的入场与旧腿维护不以交易所OI为持仓依据，仍要求有效报价。
- 新日期写入前必须满足执行/参数版本和完整期权字段校验。历史兼容限于合法历史读取；未知版本、非有限数量、数量/合约/到期/行权价矛盾均拒绝写入。
- IV反解先验证所有输入有限有效；无法计算时N/A。监测拒绝未来或严重错位行情，采用20分钟报价与标的同步容忍以兼容现有明确延迟源；仅影响监测可用性，不作用于仓位。收盘已确认行情以对应交易日收盘窗口校验，不与晚间运行时刻比较陈旧度。
- 历史本地新参数审计明确为当前规则重放，不再自称r6冻结参考。原冻结规格、输出和链不改写。
- 新构建v1.3-20260908-r7-mom120-put102-fix1；执行修复证据im_put_execution_guards_20260908_v2。上一轮失败测试、正常控制、真实报价适配、正式本地/远端入口均须重新验证。
