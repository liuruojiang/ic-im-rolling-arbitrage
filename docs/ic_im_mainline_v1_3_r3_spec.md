# IC/IM 动量分袖 v1.3 r3 对抗性修复规格

预注册日期：2026-09-03（Asia/Shanghai）  
状态：对v1.3 r2的防御性修复候选；未批准实盘；不得改写r1/r2规格、首次输出、账本或绩效产物。

## 固定范围

策略数学完全继承v1.3 r2：IC仍为中证500`MA110 / Mom24 / W2 + 50:50 Abs20`并在基础NAV回撤达到6%时减半；IM仍为中证1000`MA35 / Mom18 / W2.5 + 50:50 Abs20`并应用`Score>=150`清仓和`Volume/MA160>=0.85`。核心、网格、Put/Call、成本、30%保证金/缓冲、现金收益和执行时点不得改变；IC无Call，IM救援期限仍为`rescue_next_listed`。

## 允许修复

1. IC的6%门禁依赖自2007-01-15起连续累计的基础NAV及其历史峰值。任何实时OHLCV来源若未覆盖该正式起点和计算首日指标所需暖机数据，必须失败关闭，禁止从截短窗口重新建立NAV高水位。
2. Poe绩效续接只有在逐日实际期货名义、动量/网格仓位、Put/Call数量与合约、盯市价格和交易成本均可核验时才能标为1.3收益。现有固定1倍桥接不得冒充v1.3 r2；不能完整重建时必须明确降级到2026-08-14哈希定值终点。
3. r3账本必须以独立`strategy_revision=r3`从v1.2创世锚点按修复后规则重放；r1/r2账本只读归档，禁止原地续写。
4. 允许增加属性测试和故障注入测试，覆盖动量权重集合、T+1恒等式、哈希链篡改、截短历史、续接不完整和旧revision误接。

## 验收

- IC/IM完整本地权威数据上的目标日程与v1.3 r2逐日零差异。
- IC截短历史、错误起点或缺暖机数据全部失败关闭。
- Poe请求2026-08-14以后的绩效时，不再使用固定1倍旧桥接；在尚无完整逐腿收益账本时明确降级。
- 聚焦测试、根目录全量测试和`verify_workspace.py`全部通过。
- 修复仍为研究候选，不切换Poe、GitHub/Gmail自动化或实盘入口。
