# v1.3-r7 Put月度执行修正 v1

2026-09-07。用户授权修正正式脚本需要修正的执行逻辑；本次为本地正式研究信号入口修复，不包含云部署、邮件发送、下单或历史账本改写。

- 保持r7策略参数、季度期货规则、IC、网格、Call和资金口径。修订标识为`im_monthly_reset_20260907_v1`，构建号添加月度修复标识；账本仍为schema 3/r7，保存新信号的修订证据，已有历史记录不改写。
- IM核心及动量Put均独立月度重选约3个月、95%行权价；即使到期月份和数量相同仍生成重置动作。非月度日只调整原合约数量，不因每日目标期限漂移而自动换Put；从0仓重新进入须重新选择。
- 两腿使用相同期限选择约定：距目标日相同则选较晚实际到期日。95%参考为当前策略IM期货报价，不把中证1000指数价格代替期货价格，不使用仅预告尚未执行的远季合约报价。
- 原正式信号的T收盘/下一交易日目标展示和独立月度预告语义保留；明确输出月度计划执行日，不能把预告称为已成交。账本记录为已核验研究目标，不是实际成交账本。期货`core_eod_contract`规则不变。
- 保留正式入口原有严格合约报价门禁：规则目标不可交易或旧腿不能退出时失败关闭，不跳行权价、不伪造完成。研究引擎的可交易候选选择与正式严格门禁在不流动合约时可能不同，验收需明确区分。
- 旧r6嵌入IM收益依赖旧Put执行，默认收益查询停止将其作为有效绩效；审计读取必须显式声明历史用途。旧冻结输出、规格及哈希保留。当前正确的研究结果引用整轮结案，不能把研究结果硬塞入旧账本收益。
- 验收：正式`build_live_trade_signal`真实报价回放（月度同月换行权价、非月度不换、零仓重入、核心/动量分袖、报价缺失）；账本/日报与展示字段检查；IC、Call和期货不变的配对核验；相关回归及工作区完整性检查。测试状态隔离，真实账本不写入。
