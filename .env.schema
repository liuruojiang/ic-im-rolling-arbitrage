# @defaultSensitive=true @defaultRequired=infer

# Poe Server Bot access key. Configure in the hosting platform; never commit a value.
# @type=string @required @sensitive
POE_ACCESS_KEY=

# Durable mounted directory for latest.json and the append-only journal.
# @type=string @sensitive=false
ICIM_STATE_DIR=/data/ic_im_v1_2

# Uvicorn port.
# @type=port @sensitive=false
PORT=8000
